apiVersion: v1
kind: Deployment
metadata:
  name: react-geo
  labels:
    tier: frontend
    app: react
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react
  template:
    metadata:
      labels:
        app: react
    spec:
      containers:
        - image: maguilar22/react:geo
          name: react-plotly-geo
          ports:
            - 3000:3000

